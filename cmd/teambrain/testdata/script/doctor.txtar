# doctor under --json emits a success envelope with the retrieval status.
env XDG_CONFIG_HOME=$WORK/cfg
exec teambrain --json doctor
stdout '"ok": true'
stdout '"command": "doctor"'
stdout '"retrieval"'
! stderr .

# Human mode prints readable lines, not JSON, including the retrieval path.
exec teambrain doctor
stdout 'retrieval:'
