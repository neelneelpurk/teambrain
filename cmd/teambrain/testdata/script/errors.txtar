# Unknown command is a user error (exit 1) with a message on stderr.
! exec teambrain frobnicate
stderr 'Error:'

# A user error under --json lands a structured envelope on stdout.
env XDG_CONFIG_HOME=$WORK/cfg
! exec teambrain --json init
stdout '"ok": false'
stdout '"code": 1'
